import api from './api';
import { AgendaPagamentoDTO, AgendaPagamentoCreateDTO } from '../types'; export const agendaService = { listar ()=> { const response = await api.get('/agenda-pagamentos'); return response.data; }, obter (id)=> { const response = await api.get(`/api/agenda-pagamentos/${id}`); return response.data; }, criar (dados)=> { const response = await api.post('/agenda-pagamentos', dados); return response.data; }, atualizar (id, dados)=> { const response = await api.put(`/api/agenda-pagamentos/${id}`, dados); return response.data; }, marcarComoPago (id, dataPagamento)=> { const response = await api.patch(`/api/agenda-pagamentos/${id}/pagar`, null, { params, }); return response.data; }, cancelar (id)=> { await api.delete(`/api/agenda-pagamentos/${id}`); }, vencendoHoje ()=> { const response = await api.get('/agenda-pagamentos/vencendo-hoje'); return response.data; }, vencendoAmanha ()=> { const response = await api.get('/agenda-pagamentos/vencendo-amanha'); return response.data; }, vencendo7Dias ()=> { const response = await api.get('/agenda-pagamentos/proximos-7-dias'); return response.data; }, listarPendentes ()=> { const response = await api.get('/agenda-pagamentos/pendentes'); return response.data; }, listarAtrasados ()=> { const response = await api.get('/agenda-pagamentos/atrasados'); return response.data; },
