import api from './api';
import { SocioDTO, RetiradaDTO,
} from '../types'; export const sociosService = { // Listar todos os sócios listar ()=> { const response = await api.get('/socios'); return response.data; }, // Obter sócio por ID obter (id)=> { const response = await api.get(`/api/socios/${id}`); return response.data; }, // Criar novo sócio criar (dados)=> { const response = await api.post('/socios', dados); return response.data; }, // Atualizar sócio atualizar (id, dados)=> { const response = await api.put(`/api/socios/${id}`, dados); return response.data; },
}; export const retiradasService = { // Listar todas listar ()=> { const response = await api.get('/retiradas'); return response.data; }, // Obter retirada por ID obter (id)=> { const response = await api.get(`/api/retiradas/${id}`); return response.data; }, // Criar nova retirada criar (dados)=> { const response = await api.post('/retiradas', dados); return response.data; }, // Buscar retiradas por sócio buscarPorSocio (socioId)=> { const response = await api.get(`/api/retiradas/socio/${socioId}`); return response.data; },
