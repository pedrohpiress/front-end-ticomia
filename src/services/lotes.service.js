import api from './api';
import { LoteIngressoDTO } from '../types'; export const lotesService = { listar ()=> { const response = await api.get('/lotes-ingresso'); return response.data; }, obter (id)=> { const response = await api.get(`/api/lotes-ingresso/${id}`); return response.data; }, criar (dados)=> { const response = await api.post('/lotes-ingresso', dados); return response.data; }, atualizar (id, dados)=> { const response = await api.put(`/api/lotes-ingresso/${id}`, dados); return response.data; }, desativar (id)=> { await api.patch(`/api/lotes-ingresso/${id}/desativar`); }, buscarPorEvento (eventoId)=> { const response = await api.get(`/api/lotes-ingresso/evento/${eventoId}`); return response.data; }, listarAtivos ()=> { const response = await api.get('/lotes-ingresso/ativos'); return response.data; },
