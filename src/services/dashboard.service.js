import api from './api';
import { DashboardCompletoDTO, DashboardKPIsDTO, AlertaPagamentoDTO, ContagemRegressivaDTO,
} from '../types'; export const dashboardService = { // Obter todos os KPIs getKPIs (ano= new Date().getFullYear())=> { const response = await api.get('/dashboard/kpis', { params, // Obter dashboard completo com KPIs, alertas e gráficos getDashboardCompleto ( ano= new Date().getFullYear() )=> { const response = await api.get('/dashboard/completo', { params, // Obter top 10 vendedores por faturamento getTop10Vendedores (ano= new Date().getFullYear()) => { const response = await api.get('/dashboard/top-vendedores', { params, // Obter contagem regressiva do evento getContagemEvento ()=> { const response = await api.get('/evento/contagem-regressiva'); return response.data; }, // Obter alertas de pagamento getAlertas ()=> { const response = await api.get('/agenda-pagamentos/alertas'); return response.data; },
