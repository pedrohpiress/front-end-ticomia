import api from './api';
import { DespesaDTO, DespesaCreateDTO, PagamentoDespesaDTO,
} from '../types'; export const despesasService = { // Listar todas listar ()=> { const response = await api.get('/despesas'); return response.data; }, // Obter despesa por ID obter (id)=> { const response = await api.get(`/api/despesas/${id}`); return response.data; }, // Criar nova despesa criar (dados)=> { const response = await api.post('/despesas', dados); return response.data; }, // Atualizar despesa atualizar (id, dados)=> { const response = await api.put(`/api/despesas/${id}`, dados); return response.data; }, // Registrar pagamento de despesa pagarDespesa (id, dados)=> { const response = await api.post(`/api/despesas/${id}/pagar`, dados); return response.data; }, // Listar despesas vencendo hoje vencendoHoje ()=> { const response = await api.get('/despesas/vencendo-hoje'); return response.data; }, // Listar despesas em aberto listarAbertas ()=> { const response = await api.get('/despesas/abertas'); return response.data; },
