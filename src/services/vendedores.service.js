import api from './api';
import { VendedorDTO, VendedorCreateDTO, PageVendedorDTO } from '../types'; export const vendedoresService = { // Listar todos os vendedores listar ()=> { const response = await api.get('/vendedores'); return response.data; }, // Listar vendedores com paginação listarPaginado (page= 0, size= 10)=> { const response = await api.get('/vendedores/paginado', { params, }); return response.data; }, // Listar apenas vendedores ativos listarAtivos ()=> { const response = await api.get('/vendedores/ativos'); return response.data; }, // Buscar vendedor por ID obter (id)=> { const response = await api.get(`/vendedores/${id}`); return response.data; }, // Buscar vendedores por nome buscar (nome)=> { const response = await api.get('/vendedores/buscar', { params, // Criar novo vendedor criar (dados)=> { const response = await api.post('/vendedores', dados); return response.data; }, // Atualizar vendedor atualizar (id, dados)=> { const response = await api.put(`/vendedores/${id}`, dados); return response.data; }, // Deletar vendedor deletar (id)=> { await api.delete(`/vendedores/${id}`); }, // Toggle ativo/inativo toggleAtivo (id)=> { const response = await api.patch(`/vendedores/${id}/toggle-ativo`); return response.data; },
